# Doris 配置模板
# 复制此文件为 .env 并修改配置

# BE HTTP 地址（必需）
# 格式：host:port 或 http://host:port
# 示例：10.170.2.56:8040 或 http://10.170.2.56:8040
DORIS_BE_HTTP=10.170.2.56:8040

# 数据库名（可选，默认: video）
DORIS_DATABASE=video

# 用户名（可选，默认: devops）
DORIS_USER=devops

# 密码（必需）
DORIS_PASSWORD=SgU929SiPeLKINX!

# CORS 配置（可选）
# 允许的源，默认允许所有（*）
# CORS_ALLOWED_ORIGIN=*

# 允许的 HTTP 方法，默认：GET, POST, OPTIONS
# CORS_ALLOWED_METHODS=GET, POST, OPTIONS

# 允许的请求头，默认：Content-Type, Authorization
# CORS_ALLOWED_HEADERS=Content-Type, Authorization

# 是否允许携带凭证，默认：false
# CORS_ALLOW_CREDENTIALS=false

# 预检请求缓存时间（秒），默认：3600
# CORS_MAX_AGE=3600

# 日志配置（可选）
# 日志级别：debug, info, warn, error（默认: info）
# LOG_LEVEL=info

# 日志格式：text 或 json（默认: text）
# JSON 格式更适合生产环境和日志收集系统（如 ELK、Loki 等）
# LOG_FORMAT=text

# Gin 模式（可选）
# 可选值：debug, release, test（默认: release）
# debug 模式会输出详细的请求日志，release 模式性能更好
# GIN_MODE=release

# 调试模式（可选）
# 设置为 true 时输出详细日志，生产环境建议设为 false
# DEBUG=false


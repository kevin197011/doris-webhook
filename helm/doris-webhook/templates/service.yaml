apiVersion: v1
kind: Service
metadata:
  name: {{ include "doris-webhook.fullname" . }}
  labels:
    {{- include "doris-webhook.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "doris-webhook.selectorLabels" . | nindent 4 }}

